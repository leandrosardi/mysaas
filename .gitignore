# ignore any output file
*.out

# ignore ./deploy.sh because I use this script to shorten the ./install.rb script with all its commands  
/deploy.sh

# ignore SSL certificates, except development certificated stored on /ssl/_desa/
/ssl/*/*
!/ssl/_desa/*

# ignore the folders imported from extensions.
# reference: for more information, read #33
#
# ignore subfolders inside /views, except api*, layouts, settings, and templates 
# ignore subfolders inside /public, except core
# ignore subfolders inside /lib, except skeleton and stub 
# ignore subfolders inside /notifications
# ignore subfolders inside /p 
# ignore subfolders inside /dispatchers
# ignore subfolders inside /cli
# ignore subfolders inside /deployment-routines
# ignore subfolders inside /sql
# ignore subfolders inside /gems
# ignore subfolders inside /examples, except the /core folder
# ignore subfolders inside /docu, extept the /thumbnails folder
# ignore subfolders inside /.extensions
#
/views/*/*
!/views/api*
!/views/layouts
!/views/settings
!/views/templates
/public/*/*
!/public/core
/lib/*/*
!/lib/skeleton
!/lib/stub
/notifications/*/*
/projects/*/*
/dispatchers/*/*
/cli/*/*
/deployment-routines/*/*
/sql/*/*
/gems/*/*
/examples/*/*
!/examples/core
/docu/*/*
!/docu/thumbnails
/.extensions/*/*

# igonore the backups of the configuration files
config.*.rb
!config.template.rb

# ignore private key files
*.ppk
*.pem

# picture files in temp folders
/temp/*.jpg
/temp/*.png
/temp/*.gif
/temp/*.bmp
/cli/temp/*.jpg
/cli/temp/*.png
/cli/temp/*.gif
/cli/temp/*.bmp
/p/temp/*.jpg
/p/temp/*.png
/p/temp/*.gif
/p/temp/*.bmp

# ignore database files
*.ldf
*.mdf

# ignore lock files
*.lock

# ignore log files
*.log
*.log.data

# ignore .project file created by IDE
.project

# ignore .maintenance file used to turn the app unavailable
.maintenance

# ignore .notrial file used to switch between landing pages
.notrial

# production configuration files
config.rb
config.prod*.rb
config.backup*.rb

# backup files
*.bak 

# PID files
*.pid

# clients' storage folder
/public/clients/*/

# extensions folder
/extensions/